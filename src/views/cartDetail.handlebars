<h1 class="titulo"><i class="bi bi-cart-fill icon"></i> Carrito {{cart._id}}</h1>
<div class="container-lista">

        <div class="card-cart">
        <ul>
            <li>
            <p class="cart-category">Cantidad de categorias de productos:
                {{cart.products.length}}</p>
            <hr />
            {{#each cart.products}}
                <div class="card-img-container">
                    <img
                        class="card-img-cart"
                        src="{{this.product.thumbnail}}"
                        alt="{{this.product.title}}"
                    />
                    </div>
                <div class="cart-product">
                    <p>{{this.product.title}}</p>
                    <p>$ {{this.product.price}}</p>
                    </div>
                    <div class="cart-precio">
                    <p>Cantidad: {{this.quantity}}</p>
                    <p>Subtotal: $ {{subtotal this.product.price this.quantity}}</p>
                </div>
                <hr />
            {{/each}}
            <p class="cart-total">Total: $ {{cartTotal}}</p>
            </li>
        </ul>
        </div>
</div>